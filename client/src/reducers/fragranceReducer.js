
const initialState = {
    perfumes: [],
    modalShow: false,
    alertModalShow: false,
    currentPerfume: [],
    msg: ""
  }

  export default function(state = initialState, action) {
    switch (action.type) {
        
      default:
        return state;
    }
  }